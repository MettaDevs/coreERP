openapi: 3.1.0
info:
  title: CoreERP Management Asset API
  version: 0.1.0
paths:
  /health:
    get:
      operationId: managementAssetHealth
      responses:
        '200':
          description: Module is healthy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/hello:
    get:
      operationId: managementAssetHello
      responses:
        '200':
          description: Management Asset hello response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HelloResponse'
components:
  schemas:
    HealthResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [status, module]
          properties:
            status: { type: string, const: ok }
            module: { type: string, const: management-asset }
    HelloResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [message, module, version]
          properties:
            message: { type: string }
            module: { type: string, const: management-asset }
            version: { type: string }
