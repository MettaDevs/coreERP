openapi: 3.1.0
info:
  title: Management Asset API
  version: 0.1.0
paths:
  /api/v1/health:
    get:
      summary: Memeriksa kesiapan API
      responses:
        '200':
          description: API siap menerima request
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: object
                    required: [status]
                    properties:
                      status:
                        type: string
                        const: ready
